<% resp.forEach(blog => { %>
 <% if (blog.id===id) { %>
    <h1>
        <%= blog.title %>
    </h1>
    <img src="/upload/<%= blog.img %>" alt="<%= blog.title %>" />
    <p>
        <%= blog.des %>
    </p>
    <button id="delete" data-test="<%= blog.id %>">Delete</button>
 <% } %>
<% }) %>